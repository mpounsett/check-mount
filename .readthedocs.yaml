# .readthedocs.yml
# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for det

# required
version: 2

python:
  version: 3.8
  install:
    - method: pip
      path: .
